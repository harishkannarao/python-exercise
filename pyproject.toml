[project]
name = "python-exercise"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "flake8",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-html",
    "assertpy",
    "ruff",
]
